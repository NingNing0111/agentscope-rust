name: Documentation

on:
  pull_request:
    branches: [master]
    paths:
      - 'docs/rust/zh/**'
      - 'docs/rust/README.md'
      - 'docs/rust/mirror-map.md'
      - 'scripts/docs/**'
      - 'scripts/check-docs-mirror.sh'
      - 'tests/docs/**'
      - 'playwright.config.mts'
      - 'package.json'
      - 'package-lock.json'
      - '.nvmrc'
      - '.github/workflows/docs.yml'
  push:
    branches: [master]
    paths:
      - 'docs/rust/zh/**'
      - 'docs/rust/README.md'
      - 'docs/rust/mirror-map.md'
      - 'scripts/docs/**'
      - 'scripts/check-docs-mirror.sh'
      - 'tests/docs/**'
      - 'playwright.config.mts'
      - 'package.json'
      - 'package-lock.json'
      - '.nvmrc'
      - '.github/workflows/docs.yml'
  workflow_dispatch:
    inputs:
      run_e2e:
        description: Run Playwright browser and accessibility tests
        required: false
        type: boolean
        default: false

# 最小权限：workflow 顶层仅 contents 只读；
# 只有 deploy job 单独授予 pages: write 与 id-token: write。
permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Check, build, and test docs
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Mirror map checks
        run: scripts/check-docs-mirror.sh --quiet

      - name: Unit tests
        run: npm run test:docs

      - name: Source documentation checks
        run: npm run docs:check

      - name: Example and repository reference checks
        run: npm run docs:check-examples

      - name: Build site
        run: npm run docs:build

      - name: Check built site
        run: npm run docs:check-built

      # Browser tests are opt-in because installing Playwright system
      # dependencies can hang on transient Ubuntu mirror failures. The
      # documentation build and Node-based checks above remain required for
      # every commit and pull request.
      - name: Install Playwright Chromium
        if: ${{ inputs.run_e2e == true }}
        # The hosted Ubuntu runner already provides the shared libraries;
        # omit --with-deps so this step does not invoke apt or its mirrors.
        run: npx playwright install chromium

      - name: Browser and accessibility tests
        if: ${{ inputs.run_e2e == true }}
        run: npm run test:docs:e2e

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/rust/zh/.vitepress/dist

  deploy:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && github.ref == 'refs/heads/master')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
